/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["laminastore-admin.vercel.app", "source.unsplash.com"],
  },
};

module.exports = nextConfig;
